命令模式下：
  w 向前快速
  b 向后退
  ctrl + f 向后翻页
  ctrl + b 向后翻页
  数字 + gg 跳行，如1gg调到1行
  搜索方法：
    在命令模式下输入\ + 搜索关键词
    n 下一个搜索匹配项
    shift + n 上一个搜索匹配项
  cc 剪切掉一行
  如果错了，在命令模式下按u（undo）进行恢复
  c2c 表示要连续删除两行
  取消打错的命令， 直接按esc键
  p 粘贴
  yy 复制
  ctrl + n 内置自动补全
